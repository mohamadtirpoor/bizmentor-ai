# راهنمای تنظیم Environment Variables در لیارا

## 📧 اطلاعات ایمیل:

```
EMAIL_USER: businessmeter.ir@gmail.com
EMAIL_PASS: hqjm lhfh hmvv loyc
```

---

## 🔧 مراحل تنظیم در لیارا:

### مرحله 1: ورود به پنل لیارا
1. برو به: https://console.liara.ir/apps/bus-metr
2. از منوی سمت چپ، کلیک روی **"Settings"** (تنظیمات)

### مرحله 2: اضافه کردن Environment Variables
1. پیدا کن بخش **"Environment Variables"**
2. کلیک روی **"Add Variable"** یا **"افزودن متغیر"**

### مرحله 3: اضافه کردن EMAIL_USER
- **Key**: `EMAIL_USER`
- **Value**: `businessmeter.ir@gmail.com`
- کلیک روی **"Add"** یا **"افزودن"**

### مرحله 4: اضافه کردن EMAIL_PASS
- **Key**: `EMAIL_PASS`
- **Value**: `hqjm lhfh hmvv loyc`
- کلیک روی **"Add"** یا **"افزودن"**

### مرحله 5: Restart اپلیکیشن
1. برگرد به صفحه اصلی اپ
2. کلیک روی **"Restart"** یا **"راه‌اندازی مجدد"**
3. صبر کن تا اپ restart بشه (حدود 30 ثانیه)

---

## ✅ تست کردن:

بعد از restart، می‌تونی تست کنی:

```bash
curl -X POST https://businessmeter.ir/api/auth/send-code \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com"}'
```

اگه موفق باشه، باید این پیام رو ببینی:
```json
{
  "success": true,
  "message": "کد تایید به ایمیل شما ارسال شد"
}
```

---

## 🎯 مرحله بعدی:

بعد از تنظیم Environment Variables، باید Frontend رو آپدیت کنیم که:
1. فرم ورود با ایمیل داشته باشه
2. دکمه "ارسال کد" داشته باشه
3. فیلد ورود کد 6 رقمی داشته باشه
4. تایمر 10 دقیقه‌ای داشته باشه

---

## ⚠️ نکات مهم:

1. **فاصله‌ها مهم هستن**: رمز دقیقاً باید `hqjm lhfh hmvv loyc` باشه (با فاصله‌ها)
2. **Restart اجباری**: بعد از اضافه کردن متغیرها، حتماً restart کن
3. **امنیت**: این اطلاعات رو با کسی به اشتراک نذار
4. **محدودیت Gmail**: حداکثر 500 ایمیل در روز

---

## 📞 مشکل داری؟

اگه بعد از تنظیم، ایمیل ارسال نشد:
1. چک کن Environment Variables درست set شدن
2. چک کن اپ restart شده
3. لاگ‌های لیارا رو ببین: `liara logs --app bus-metr -f`
4. بهم بگو تا کمکت کنم!
