# راهنمای آپلود PDF ها به سرور لیارا

## ✅ وضعیت فعلی
- کد دیپلوی شد: https://businessmeter.ir
- PDF ها هنوز آپلود نشدن (حجم زیاد بود)
- سیستم RAG فعلاً غیرفعاله

## 📋 مراحل آپلود PDF ها

### روش 1: استفاده از Liara Disk + FTP (توصیه می‌شه)

#### قدم 1: ایجاد Disk
1. برو به: https://console.liara.ir/apps/bus-metr/disks
2. کلیک روی "ایجاد دیسک جدید"
3. تنظیمات:
   - نام: `pdf-storage`
   - حجم: `1` GB
   - مسیر Mount: `/app/Sorce`
4. ذخیره کن (اپ restart میشه، نگران نباش)

#### قدم 2: آپلود با FTP
1. دانلود FileZilla: https://filezilla-project.org/
2. از پنل لیارا اطلاعات FTP رو بگیر
3. وصل شو و پوشه `Sorce` رو آپلود کن

### روش 2: استفاده از Liara Shell + wget

```bash
# 1. فایل‌ها رو فشرده کن (در سیستم خودت)
cd "D:\مشاوره کسب و کار"
tar -czf sorce.tar.gz Sorce/

# 2. فایل رو آپلود کن به یک سرویس موقت (مثل file.io یا transfer.sh)
# مثال با PowerShell:
curl.exe -F "file=@sorce.tar.gz" https://file.io

# 3. وصل شو به Shell لیارا
liara shell --app bus-metr

# 4. در Shell لیارا:
cd /app
wget [URL که از file.io گرفتی]
tar -xzf sorce.tar.gz
rm sorce.tar.gz
ls -la Sorce/  # بررسی کن فایل‌ها آپلود شدن
exit

# 5. Restart کن
liara restart --app bus-metr
```

### روش 3: استفاده از Object Storage (پیشرفته)

اگه روش‌های بالا کار نکرد، می‌تونی از Object Storage استفاده کنی.
(نیاز به تغییر کد داره)

## 🧪 تست سیستم RAG

بعد از آپلود:
1. برو به: https://businessmeter.ir
2. یکی از Expert Mode ها رو انتخاب کن
3. یک سوال تخصصی بپرس
4. اگه RAG کار کنه، باید از منابع PDF استفاده کنه

## 📊 ساختار پوشه‌ها

```
Sorce/
├── Finance/      (4 فایل PDF)
├── HR/           (5 فایل PDF)
├── Marketing/    (110+ فایل PDF)
├── product/      (8 فایل PDF)
└── Seles/        (5 فایل PDF)
```

## ⚠️ نکات مهم

- حجم کل: ~270MB
- Disk 1GB کافیه
- بعد از آپلود، نیازی به دیپلوی مجدد نیست
- فقط یکبار باید آپلود کنی
- اگه Disk استفاده کنی، فایل‌ها permanent هستن

## 🔍 بررسی لاگ‌ها

```bash
# دیدن لاگ‌های زنده
liara logs --app bus-metr -f

# یا از پنل
https://console.liara.ir/apps/bus-metr/logs
```

## ❓ مشکل داری؟

اگه هر کدوم از روش‌ها کار نکرد، بهم بگو تا راه دیگه‌ای پیدا کنیم.
